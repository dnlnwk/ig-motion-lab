# Motion Lab by Daniel Nowak

A collection of UI micro-interactions spotted around the web (e.g. Instagram), recreated in React/Next.js.

Each interaction lives in its own route under `src/app/`, with technical details in the README next to its code.